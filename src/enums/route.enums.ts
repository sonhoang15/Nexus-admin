export enum ERoutePath {
  AUTH = "/auth",

  DASHBOARD = "/",
  USERS = "/users",
  CATEGORIES = "/categories",
  PRODUCTS = "/products",
  DOCUMENTS = "/documents",
  PAGES = "/pages",
  SETTINGS = "/settings",

  NOT_FOUND = "*",
}
